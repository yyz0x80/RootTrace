# Use a comma in the greeting

Change `environment_demo.greeting` so `greeting("Ada")` returns
`"Hello, Ada!"`. Preserve the function signature, keep tests read-only, and
change only `environment_demo/__init__.py`.

