# Sort tasks by priority

Change `TaskList.all()` so that important tasks appear first. Tasks with the
same importance should be ordered appropriately for users.

Do not change the public method names.

